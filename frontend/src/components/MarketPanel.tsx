import type { Analysis } from '../api/client'
export function MarketPanel({ data }: { data: Analysis }) { const { market, indicators } = data; return <section className="card hero"><div><p className="eyebrow">Live mock snapshot</p><h2>{market.symbol.replace('_', ' / ')}</h2><p className="price">{market.bid.toFixed(5)} <span>bid</span></p></div><div className="quote"><b>{market.ask.toFixed(5)}</b><span>ask</span><small>Spread {market.spread.toFixed(5)}</small></div><div className="metrics"><div><span>Regime</span><b className="positive">{indicators.regime}</b></div><div><span>Session</span><b>{market.session}</b></div><div><span>RSI</span><b>{indicators.rsi}</b></div><div><span>ATR</span><b>{indicators.atr}</b></div></div></section> }
