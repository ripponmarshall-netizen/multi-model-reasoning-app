import type { JournalEntry } from '../api/client'
export function TradeJournal({ entries }: { entries: JournalEntry[] }) { return <section className="card journal"><div className="line"><div><p className="eyebrow">Convex journal</p><h3>Paper trades</h3></div><span className="pill">{entries.length} entries</span></div>{entries.length === 0 ? <p className="empty">No paper orders yet. Run analysis and submit an approved setup.</p> : <div className="table"><table><thead><tr><th>Time</th><th>Pair</th><th>Side</th><th>Units</th><th>Entry</th><th>Status</th></tr></thead><tbody>{entries.map(entry => <tr key={entry.id}><td>{new Date(entry.created_at).toLocaleTimeString()}</td><td>{entry.symbol}</td><td>{entry.side}</td><td>{entry.units.toLocaleString()}</td><td>{entry.entry_price}</td><td><span className="pill good">{entry.status}</span></td></tr>)}</tbody></table></div>}</section> }
