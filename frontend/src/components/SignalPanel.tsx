import type { Analysis, LLMAnalysis } from '../api/client'
function Model({ name, analysis }: { name: string; analysis: LLMAnalysis }) { return <article className="model"><div className="line"><strong>{name}</strong><span className={`pill ${analysis.trade_allowed ? 'good' : 'bad'}`}>{analysis.bias}</span></div><p>{analysis.reason}</p><small>Confidence {Math.round(analysis.confidence * 100)}%</small></article> }
export function SignalPanel({ data }: { data: Analysis }) { return <section className="card"><p className="eyebrow">Deterministic strategy</p><div className="line"><h3>{data.candidate.side.toUpperCase()} setup</h3><span className="pill">{data.indicators.regime}</span></div><p>{data.candidate.rationale.join(' · ')}</p><div className="trade-grid"><span>Entry <b>{data.candidate.entry_price}</b></span><span>Stop <b>{data.candidate.stop_loss}</b></span><span>Target <b>{data.candidate.take_profit}</b></span></div><div className="models"><Model name="ChatGPT / OpenAI" analysis={data.consensus.openai}/><Model name="Gemini" analysis={data.consensus.gemini}/></div></section> }
